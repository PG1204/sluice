# testdata

Sample CSV and Parquet fixtures used by engine and storage tests.

Populated in Phase 2 (storage layer). Keep fixtures small and committed so the
test suite is hermetic — no network, no generation step.
